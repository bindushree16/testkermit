# for resources image 

resources:
  - name: imgs
    type: image
    imageName: drydock/u18node #shippabledocker/u16phpall
    configuration:
      integrationName: dockerInt
    version:
      imageTag: master
 
      
pipelines:
  - name: pipeline
    steps:
      - name: s1
        type: bash
#         triggeredBy:
#         requires:
#           resources:
#             - imgs
        execution:
          onExecute:
            - printenv
            - write_output imgs env1=key  # imageTag=$STEP_ID
        outputResources:
          - imgs

      - name: s2
        type: bash
        triggeredBy:
          resources:
            - imgs
        execution:
          onExecute:
             - echo $env1 
#             - echo $res_imgs_imageTag 
            
        
#       - name: s3
#         type: bash
#         requires:
#           resources:
#             - imgs
#         execution:
#           onExecute:
#             - echo $res_imgs_imageTag 
        
        
 
